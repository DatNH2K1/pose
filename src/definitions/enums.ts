export enum Side {
    TOP = 'top',
    BOTTOM = 'bottom',
    RIGHT = 'right',
    LEFT = 'left'
}

export enum Mode {
    BASIC = 'basic'
}

export enum Pose {
    FRONT = 'front',
    LEFT_SIDE = 'leftSide',
    RIGHT_SIDE = 'rightSide',
    LEFT_SIT = 'leftSit',
    RIGHT_SIT = 'rightSit',
    BACK = 'back'
}